<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login Page</title>
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div class="login-container">
    <h2 style="color:white;">Enter at your own risk</h2>
    <form action="your-page-url.html" method="post"> <!-- Change your-page-url.html to the URL of your desired page -->
      <input type="text" placeholder="Username" required><br>
      <input type="password" placeholder="Password" required><br>
      <!-- Wrap the button inside an anchor tag -->
    </form>
    <nav>
      <!-- Conditionally render login or logout links -->
      {{#if logged_in}}
      <a href="/profile">profile</a> |
      <button class="no-button" id="logout">logout</button>
      {{else}}
      <a href="your-page-url.html"><input type="submit" value="Login" id="loginBtn"></a> 
      <!-- Sign up button -->
      <a href="signup.html"><input type="sign" value="Signup" id="signupBtn"></a> 
      {{/if}}
    </nav>
    </header>
    <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>

    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
  </div>
</body>

</html>
